<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainKeyboard"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:background="@drawable/mainbg">

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_marginLeft="105dp"
        android:layout_marginBottom="10dp"
        android:src="@drawable/mainkeyb" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="~ Add Keyboard Data ~"
        android:fontFamily="casual"
        android:textSize="20sp"
        android:textColor="@color/white"
        android:textAlignment="center"
        android:gravity="center_horizontal"
        android:layout_margin="10dp"
        android:textStyle="bold"
        />
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_code"
        android:hint="Keyboard Code"
        />
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_brand"
        android:hint="Keyboard Brand"/>
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_name"
        android:hint="Keyboard Name"/>
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_price"
        android:hint="Keyboard Price"/>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">
        <Button
            style="@style/btn_Style"
            android:id="@+id/btn_submit"
            android:text="ADD"
            android:onClick="addData"/>
        <Button
            android:id="@+id/btn_show"
            style="@style/btn_Style"
            android:text="Show All"
            android:onClick="showAll"/>

    </LinearLayout>


</LinearLayout>