<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".UpdateMouse"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:background="@drawable/mainbg">

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_marginLeft="105dp"
        android:layout_marginBottom="10dp"
        android:src="@drawable/mainmouse" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="~ Update Mouse Data ~"
        android:textSize="20sp"
        android:textAlignment="center"
        android:textColor="@color/white"
        android:fontFamily="casual"
        android:gravity="center_horizontal"
        android:layout_margin="10dp"
        android:textStyle="bold"
        />
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_codeu"
        android:hint="Mouse Code"
        android:editable="false"
        />
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_brandu"
        android:hint="Mouse Brand"/>
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_nameu"
        android:hint="Mouse Name"/>
    <EditText
        style="@style/edit_text"
        android:id="@+id/input_priceu"
        android:hint="Mouse Price"/>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">
        <Button
            style="@style/btn_Style"
            android:id="@+id/btn_update"
            android:text="Update"
            android:onClick="updateData"/>
        <Button
            android:id="@+id/btn_cancel"
            style="@style/btn_Style"
            android:text="Cancel"
            android:onClick="cancelData"/>

    </LinearLayout>


</LinearLayout>